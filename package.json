{
  "name": "bus-in-car-park",
  "version": "0.0.0",
  "license": "MIT",
  "bin": {
    "bicp": "bin/bicp"
  },
  "scripts": {
    "ci": "npm test && npm run lint",
    "docs": "webpack && npm run jsdoc && cd docs/extras && make",
    "fix": "standard --fix",
    "jsdoc": "jsdoc src README.md --recurse --destination ./docs/ --configure jsdoc.json",
    "lint": "standard",
    "test": "mocha"
  },
  "dependencies": {
    "chai": "^4.1.2",
    "chai-properties": "^1.3.0",
    "jsdoc": "^3.5.5",
    "mocha": "^4.0.1",
    "ramda": "^0.25.0",
    "standard": "^10.0.3",
    "webpack": "^3.10.0"
  }
}
